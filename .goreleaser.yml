before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  -
    goos:
      - linux
      - windows
      - darwin
      - freebsd

    goarch:
      - amd64

    binary: lss

    main: ./cmd/lss.go

    ldflags: -s -w -X sat-stack/version.GitCommit={{.ShortCommit}} -X sat-stack/version.Build=release

    env:
      - GIN_MODE=release

checksum:
  name_template: '{{.Binary}}_{{.Version}}_checksums.txt'

snapshot:
  name_template: SNAPSHOT-{{ .Commit }}

archives:
  -
    name_template: "lss_{{.Version}}_{{.Os}}-{{.Arch}}"
    replacements:
      darwin: macOS
      linux: Linux
      windows: Windows
      freebsd: FreeBSD

    files:
      - LICENSE

    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip

release:
  github:
    owner: onyb
    name: sat-stack
